# Operator-private runtime and managed clones — never committed.
/state/
/repos/
/data/
/.env

# Local review surfaces and scratch.
/.lavish/
*.log

# Personal/local Claude Code permission overrides — never committed into the
# shared distro.
/.claude/settings.local.json

# Editor / OS cruft.
.DS_Store
node_modules/
