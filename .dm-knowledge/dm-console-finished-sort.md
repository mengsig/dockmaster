- **[invariant]** The console's 'Recently finished' ledger group (ui/public/views.mjs LEDGER_GROUPS, comparator carried in the tuple) sorts by w.last_signal_at newest first, title tie-break, missing/empty stamps sorting last. `last_signal_at` is the task's last recorded event: the landing append from dm-pr.sh / dm-merge.sh when the dockmaster landed it, but for a `merge_authority=never` repo where the operator merges on GitHub, dm-pr.sh's check only records pr_state=MERGED with no append - there the stamp is the PR-open event, a lower bound on completion, not the completion time itself. Falls back to created when no event ever landed. The group also swaps its ledger column to 'Finished' (`ago(w.last_signal_at)`) instead of 'Started', since the ledger's only other timestamp is start time and start/finish order can disagree. Task order otherwise comes straight from dm_all_task_ids' glob order (bin/dm-lib.sh), so any other ledger group is still task-id order (the shell sorts the glob), not completion order.  _(2026-07-27T11:56:46Z)_
