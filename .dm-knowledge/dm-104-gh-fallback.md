- **[convention]** GitHub CLI resolution (#104): plain `gh` is the supported baseline for every toolbelt GitHub call; `gh-axi` is operator-private (no public install) and only ever PREFERRED for the three mutations. Mutations resolve via `dm_require_github_cli` (dm-lib); `jq`-parsed reads always call `gh api` directly because `gh-axi api` emits YAML. One argv cannot serve both: `gh-axi` takes the HTTP method positionally (`api PUT <p> --field`) and does NOT support `--raw-field` (it silently sends an EMPTY body, which would merge without the sha condition), while `gh` needs `api --method PUT <p> --raw-field`. Build argv per binary; never swap the name alone.  _(2026-07-21T19:16:13Z)_
- **[pitfall]** Silent `set -e` abort in a capture: `x="$(... | grep ... | head -n1)"` under `set -euo pipefail` kills the script with NO message — exit 1 on no match, exit 141 when head SIGPIPEs grep (measured: 2 matches never trips it, ~50k does, so it is buffer-dependent). Any `[ -n "$x" ] || dm_die` below it is unreachable dead code. Use `grep -m1 ... <<<"$var" || true` (no pipe, no SIGPIPE class). This shipped in BOTH dm-pr.sh open and dm-repo.sh create, each stranding real irreversible work (a pushed branch + open PR; a created GitHub repo) with nothing recorded — when you find a shape like this, grep the whole toolbelt before moving on.  _(2026-07-21T19:16:25Z)_
- **[pitfall]** AGENTS.md byte budget: `tests/runtime-performance.js` caps AGENTS.md at `shared_agents_bytes` + 2048 from a STALE `base_commit`. As of eda4f2d main sits at 28675/28682 — ~7 bytes free, so essentially any AGENTS.md edit now fails the guard. Put new durable facts in `.dm-knowledge/` (the documented current home; the AGENTS.md dm:knowledge block is legacy/migration) rather than relaxing the allowance. Re-baselining `shared_agents_bytes` to current main is a legitimate separate maintenance act, but keep the 2048 allowance.  _(2026-07-21T19:16:25Z)_
