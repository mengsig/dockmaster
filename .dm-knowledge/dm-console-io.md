- **[invariant]** The console's own /review/<id>/ page is the PRIMARY destination for a review; the lavish session (4387) is a secondary 'the original' link. They are different things: the served page is the archived directory dm-lavish.sh list reports, the session is a separate server. Neither may be presented as the other - with no session open the secondary says so instead of redirecting to the archive.  _(2026-07-29T10:53:27Z)_
- **[decision]** The console NEVER edits a crewmate's review HTML. An earlier cut appended a "back to the console" anchor after the artifact; that put console chrome inside a document the crewmate controls (an unclosed <script>/<style> swallows it, and the page's own DOM can move or restyle it). The way back rides the SHELL (server.js reviewShellHtml, `.review-back`), outside the sandboxed frame. The artifact is passed through byte for byte.  _(2026-07-29T12:20:00Z)_
- **[invariant]** An archived artifact cannot reach the network, and reviewCsp() is only half of why - measured in a browser against an artifact that tries every channel. fetch/XHR: connect-src 'none'. Subresources: scoped to /review/. Forms: form-action 'none'. top.location and window.open: sandbox="allow-scripts" with neither allow-top-navigation nor allow-popups. NAVIGATING ITS OWN FRAME is the one channel no directive on the artifact's own document governs, and what closes it is `frame-src 'self'` on the SHELL (enforced on every later navigation of that frame) plus frame-ancestors 'none' on every console response. Do not widen reviewShellCsp()'s frame-src.  _(2026-07-29T12:20:00Z)_
- **[pitfall]** dm-backlog.sh hold is an UPSERT keyed on the key: it preserves status/answer but OVERWRITES question, options and the --origin backlink dm-trash.sh resolves holds through. Keys are free-form and invented per question, so a second writer under the same key destroys the first decision with no trace. dm-ui.sh ask therefore writes only where it can write nothing away - no hold, or a hold byte-identical to the one it would write (the retry case).  _(2026-07-29T12:20:00Z)_
- **[convention]** dm-ui.sh ask <key> "<question>" [--options "A | B"] is how the dockmaster asks the operator through the page instead of the terminal: a decision hold (durable, rendered by the Needs-you panel) plus a transcript post. The answer returns on the ordinary poll queue as an operator message opening 'Answer — <question>'. No second transport, and the poll claim protocol is untouched. The question must be one line, and '|' separates options so it cannot appear inside one.  _(2026-07-29T12:20:00Z)_
- **[invariant]** The transcript is the only durable record that a page-answered question was answered - the hold stays OPEN until the dockmaster resolves it, and the Needs-you panel is rebuilt from scratch every 30s. dom.mjs ANSWER_HEADER is the shared identity both sides compose, so console.mjs finds the answer by exact prefix match and the row stays answered across a rebuild, a reload and a console restart instead of offering the buttons a second time.  _(2026-07-29T12:20:00Z)_
- **[pitfall]** A worded page (server.js sendWordedPage) styles itself with an inline <style>, which the console CSP's `style-src 'self'` drops - the page then renders unstyled. It carries its own WORDED_CSP: stricter than the console's everywhere (no script, no img, no connect) and relaxed only for that one server-authored inline style.  _(2026-07-29T12:20:00Z)_
