- **[invariant]** dm-command-guard.sh is an ALLOWLIST, not a denylist (#121/#105): check_git_subcommand is the single owner of the verdict and refuses any subcommand not named permitted, so unknown/future subcommands fail closed. Wrapper unwrapping (is_command_wrapper) is a PRECISION aid only - the safety boundary is check_stray_git_tokens, which refuses any unrecognized executable holding a git token, so an unlisted wrapper fails closed instead of bypassing. xargs is deliberately NOT a wrapper: stdin makes its argv unknowable, so it must be refused rather than classified on incomplete input. Deliberate permissions (--force-with-lease per #89, rebase/merge/pull, uninvoked aliases, inert text tools) each have a smoke test pinning them. Extend the allowlist, never re-add a denylist branch.  _(2026-07-21T04:45:31Z)_
- **[invariant]** The guard's execution-vector rules (rebase --exec, bisect run, submodule foreach, difftool --extcmd, alias shadowing, config_key_executes) NARROW that class, they do not close it - do not write a claim that they do. Git keeps adding executing config keys, config_key_executes matches a moving target by pattern, and a subcommand that grows a new command-running option gains it silently. The honest claim, and the one SECURITY.md makes: the guard raises the cost of an ACCIDENTAL destructive command and catches what an agent actually emits; it is not a sandbox and does not resist someone who knows it is there, because it parses one command rather than interpreting a shell.  _(2026-07-21T05:30:00Z)_
- **[pitfall]** The guard's lexer must treat a redirection operator as part of its token: `&` was a segment terminator, so `git push origin main 2>&1 --force` stranded --force in a phantom segment whose executable was `1` and force-updated a real remote. `>` alone never split. Any new operator handling must keep `&&` and background `&` terminating while `2>&1`, `>&2` and `&>log` stay in-segment. Equally: has_arg must match `--opt=value` as well as `--opt` (git accepts both, and --exec=/--extcmd= executed while the -x spelling was refused), and git config names are CASE-INSENSITIVE so config keys must be lowercased before matching.  _(2026-07-21T05:30:00Z)_
- **[invariant]** dm_lock's reclaim marker (<file>.lock.reclaim) must never become a permanent wedge (#122): it records its owner PID and self-heals two ways - a dead recorded owner, or (when unstamped) having blocked a waiter for DM_LOCK_RECLAIM_STALL_SPINS spins. Age is valid evidence for the MARKER only because its critical section is bounded and tiny; it is NOT valid for the lock itself, which reclaims solely on a dead recorded PID. The ~30s timeout message must name both the lock dir and the marker, or an operator removes the wrong one.  _(2026-07-21T04:45:31Z)_
- **[invariant]** A pre-subcommand git option must be guarded in BOTH spellings, and every parser walking that token position must agree. `--exec-path=DIR` (twin of the refused GIT_EXEC_PATH env var) bypassed the guard because git_subcommand_index's `-*` catch-all silently skipped any option it did not name, while check_environment_prefixes refused the identical redirect. git_option_class now classifies value/flag/refused/unknown and fails closed on unknown, mirroring verb_option_class. Adding a rule means sweeping EVERY parser in that position (git_subcommand_index, segment_command_index, check_environment_prefixes), not only the one that reproduced the bug. git_subcommand_index sets a global rather than printing: its caller uses `|| return 0`, which in a command substitution would swallow a deny's exit and turn a refusal into a PASS.  _(2026-07-21T20:09:29Z)_
- **[pitfall]** The guard's lexer models a SHELL, so shell scoping binds it: `#` ends a comment at end of LINE, not end of INPUT. Breaking out of the lexer loop on `#` discarded every later newline-separated command unguarded, so a bare `#` + newline + `git push --force` PASSED. Skip to the next newline and emit a `;`; never break. Load-bearing in the other direction too - OVER-blocking is what gets a guard switched off, so a quoted MULTI-WORD string starting with `git` is re-entered into the guard and classified, not refused on sight (`--body "git log shows the bug"` is not an attack). A BARE `git` token still refuses outright: the wrapper's real argv is the rest of the segment, which that token does not describe.  _(2026-07-21T20:09:29Z)_
- **[invariant]** The both-spellings rule applies to ENV VARS as well as options: Git falls back to the plain-spelled variable when the `GIT_*` one is unset, so guarding only `GIT_PAGER`/`GIT_EDITOR`/`GIT_ASKPASS` left `PAGER=`, `EDITOR=`, `VISUAL=`, `SSH_ASKPASS=` executing (all four verified against git 2.54 - PAGER and the editor family by payload marker under a pty, SSH_ASKPASS against a 401 server). Adding any `GIT_*` rule means checking whether git has an unprefixed fallback and covering both. Negative results worth keeping: LESSOPEN/LESSCLOSE do NOT fire (git PIPES to the pager, and less only preprocesses NAMED input files), and SHELL does not.  _(2026-07-21T20:32:57Z)_
- **[pitfall]** `-C <dir>` reaches another repository - hence another config and another set of hooks - exactly as the refused `--git-dir` does, but the toolbelt uses it constantly so refusing it is not viable. It is a DELIBERATE EXCEPTION and both the guard header and SECURITY.md must say so; do not let the "refused in both spellings" sentence be read as covering it.  _(2026-07-21T20:32:57Z)_
- **[pitfall]** A first-word test is the wrong shape for classifying a quoted command string: `sh` ignores leading whitespace and the first word may legitimately be an env assignment, a wrapper, or a shell, so ` git push --force`, a tab-separated form, `env git ...` and `timeout 5 git ...` all walked past a literal leading-`git` check. Trigger re-entry on anything that BEGINS a command (token_begins_a_command: `*=*`, git, eval, busybox, is_command_wrapper, is_shell_executable) and hand the whole string to the normal segmentation, which already unwraps wrappers and nested shells. Known residual false positive, accepted: prose whose first word is a wrapper name AND which contains a bare `git` word (`"nice work on the git fix"`) refuses.  _(2026-07-21T20:32:57Z)_
