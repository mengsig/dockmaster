- **[invariant]** dm-command-guard.sh is an ALLOWLIST, not a denylist (#121/#105): check_git_subcommand is the single owner of the verdict and refuses any subcommand not named permitted, so unknown/future subcommands fail closed. Wrapper unwrapping (is_command_wrapper) is a PRECISION aid only - the safety boundary is check_stray_git_tokens, which refuses any unrecognized executable holding a git token, so an unlisted wrapper fails closed instead of bypassing. xargs is deliberately NOT a wrapper: stdin makes its argv unknowable, so it must be refused rather than classified on incomplete input. Deliberate permissions (--force-with-lease per #89, rebase/merge/pull, uninvoked aliases, inert text tools) each have a smoke test pinning them. Extend the allowlist, never re-add a denylist branch.  _(2026-07-21T04:45:31Z)_
- **[invariant]** The guard's execution-vector rules (rebase --exec, bisect run, submodule foreach, difftool --extcmd, alias shadowing, config_key_executes) NARROW that class, they do not close it - do not write a claim that they do. Git keeps adding executing config keys, config_key_executes matches a moving target by pattern, and a subcommand that grows a new command-running option gains it silently. The honest claim, and the one SECURITY.md makes: the guard raises the cost of an ACCIDENTAL destructive command and catches what an agent actually emits; it is not a sandbox and does not resist someone who knows it is there, because it parses one command rather than interpreting a shell.  _(2026-07-21T05:30:00Z)_
- **[pitfall]** The guard's lexer must treat a redirection operator as part of its token: `&` was a segment terminator, so `git push origin main 2>&1 --force` stranded --force in a phantom segment whose executable was `1` and force-updated a real remote. `>` alone never split. Any new operator handling must keep `&&` and background `&` terminating while `2>&1`, `>&2` and `&>log` stay in-segment. Equally: has_arg must match `--opt=value` as well as `--opt` (git accepts both, and --exec=/--extcmd= executed while the -x spelling was refused), and git config names are CASE-INSENSITIVE so config keys must be lowercased before matching.  _(2026-07-21T05:30:00Z)_
- **[invariant]** dm_lock's reclaim marker (<file>.lock.reclaim) must never become a permanent wedge (#122): it records its owner PID and self-heals two ways - a dead recorded owner, or (when unstamped) having blocked a waiter for DM_LOCK_RECLAIM_STALL_SPINS spins. Age is valid evidence for the MARKER only because its critical section is bounded and tiny; it is NOT valid for the lock itself, which reclaims solely on a dead recorded PID. The ~30s timeout message must name both the lock dir and the marker, or an operator removes the wrong one.  _(2026-07-21T04:45:31Z)_
