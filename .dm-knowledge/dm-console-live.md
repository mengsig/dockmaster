- **[pitfall]** ui/live.js progressNote() must locate a task's real status note via last_event (the verb) as a marker into state_detail, not by branching on task.state - state_detail embeds the crewmate's own 'verb: note' line either raw or nested inside dm-task.sh's reconcile prose depending on which branch of 'dm-task.sh state' produced it, and branching on state alone silently drops the note for whichever states aren't special-cased (missed working/ready_for_review entirely; 48/48 open tasks rendered note: '').  _(2026-07-26T19:03:15Z)_
- **[pitfall]** task.state === 'done' is unambiguous in ui/live.js: bin/dm-task.sh state has exactly three early-exit branches that produce it (:313 pr merged, :317 scout report.md, :327 a status-log grep for a merged: event), all BEFORE the verb-switch that embeds the crewmate's own 'verb: note' line - so state_detail for a done task is landing evidence (a PR URL, a report path, or the literal string 'landed'), never a status line. A note-extraction fallback that treats 'verb present but marker not found' as a read failure will fire for every done task (measured: 49/99 on the real fleet) unless it short-circuits on task.state === 'done' first.  _(2026-07-26T19:23:41Z)_
