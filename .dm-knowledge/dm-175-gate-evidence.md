- **[invariant]** Gate evidence on a PR is COLLECTED, never re-decided: bin/dm-evidence.sh only calls each gate's own `evidence <id>` subcommand, and dm-verify.sh's evidence reuses gate_decision — the same function `gate` exits on — so a PR body can never state a verdict the gate did not make. A producer that errors or is missing renders a stated 'evidence unavailable' line; only a gate that genuinely did not run contributes nothing.  _(2026-07-25T14:36:02Z)_
- **[pitfall]** gh CANNOT upload an image to GitHub's user-attachments CDN (no supported API), so PR evidence is text only. Separately verified: dm-pr.sh open never composes the body itself — it hands the CLI --body-file <path> or --body <string>, and the operator's gh-axi wrapper rewrites --body-file into --body <contents>. Appending therefore means composing a NEW temp body file (state/.pr-body.*), never mutating the caller's file, and stripping any previously appended section first so a re-open cannot stack blocks.  _(2026-07-25T14:36:17Z)_
- **[pitfall]** The appended section is delimited at BOTH ends (`<!-- dm:gate-evidence -->` … `<!-- /dm:gate-evidence -->`) and dm_evidence_strip removes only a COMPLETE region that ends the body. Anchoring on the opening marker alone truncated any body that merely quoted it — a PR description about this machinery being the obvious case — silently discarding the operator's text from that line down. Duplicating a section is visible; eating a description is not. Matching ignores a trailing \r so a body round-tripped through GitHub (CRLF) still strips.  _(2026-07-25T19:30:00Z)_
- **[invariant]** dm-lib.sh owns the section's shape (DM_EVIDENCE_BEGIN/END, dm_evidence_wrap, dm_evidence_strip), not dm-evidence.sh: dm-pr.sh must be able to strip and to render an "evidence UNAVAILABLE" section when the collector itself is missing or exits non-zero. A gate that did not run must be visible ON THE PR; a warning on stderr reaches a log nobody opens, and a PR carrying nothing is indistinguishable from one whose gates all legitimately stayed out.  _(2026-07-25T19:30:00Z)_
- **[pitfall]** Flow notes are crew-written free text landing in a GitHub-rendered table. `|` and `<` are neutralised as HTML entities (`&`, `\` with them) — escaping `|` as `\|` was the trap: a note already containing `\|` became `\\|`, a rendered backslash plus a LIVE delimiter. Flow name/result need no escaping (validated to [A-Za-z0-9._-] and pass|fail|flake) and dm_require_single_line on the note is what stops a note forging a row or a marker line of its own.  _(2026-07-25T19:30:00Z)_
