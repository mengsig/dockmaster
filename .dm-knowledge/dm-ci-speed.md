- **[decision]** tests/smoke.sh is sharded by CONTIGUOUS section groups (# shard:split markers) because it is one linear script over one $DM_HOME; --shard k/n slices the file with awk and execs the slice, since only 32% of the run is inside check bodies. Shared fixtures live in the prelude; shared STATE lives in # shard:bootstrap sections that re-run in every later shard without their checks being counted. Adding a section is free; moving a split marker is not.  _(2026-07-26T21:10:49Z)_
- **[pitfall]** dm-status.sh cost grows with the task count in a DM_HOME: 0.6s with 5 tasks, ~9s late in the smoke run. That is why merging two shard groups measured WORSE than the sum of their parts, and why the suite's own late sections were its slowest.  _(2026-07-26T21:10:57Z)_
- **[command]** Run the suite as tests/smoke-parallel.sh (all shards at once, ~66s on 16 cores) instead of tests/smoke.sh (~401s); tests/smoke.sh --shards prints the shard count, --shard k/n runs one group.  _(2026-07-26T21:11:02Z)_
