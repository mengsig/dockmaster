- **[decision]** Removed the computed dispatch-recommendation machinery (`dm_recommended_dispatch`, `dm-task.sh recommend`, `model_recommended`/`effort_recommended` meta, and the sizing_reason upsize gate this task first added) per operator direction: no default to defer to, sizing is the dockmaster's judgment call from the brief's clarity and the work's stakes. `task-lifecycle` now states it as one combined ladder (`haiku·low` ... `opus·xhigh`); review keeps its `opus·high` floor as the one exception. `dm-task.sh sizing` is back to a plain recorded-choice report (no unsized breakdown, since nothing to compare against remains). See `.dm-knowledge/lifecycle.md` for the durable version of this fact.  _(2026-07-27T00:00:00Z)_
