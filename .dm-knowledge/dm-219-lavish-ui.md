- **[invariant]** dm-lavish.sh poll's STDOUT is operator feedback and nothing else - the console relays every byte of it onto the operator's message queue, so any guidance the script itself prints (the lavish-axi-absent degrade) must go to stderr or it arrives as something the operator said. poll also execs lavish-axi, so the pid a caller captured IS the poller: left as a child, a SIGTERM aimed at the wrapper orphans the poll.  _(2026-07-30T05:43:30Z)_
- **[decision]** The console's in-page review overlay frames the ARCHIVED ARTIFACT (/review/<id>/<file>) directly, never the /review/<id>/ shell - the shell keeps frame-ancestors 'none' and is untouched, and the console's own chrome (listening indicator, Approve/Request changes, Close) sits outside the sandboxed frame. The cross-origin lavish session (4387) is NOT embedded: its RTCPeerConnection side channel is unmitigable from here because our CSP does not govern another server's responses, so 'the original' stays an explicit new-tab link.  _(2026-07-30T05:43:30Z)_
- **[invariant]** A review listener is armed on open and stopped on close, but the keepalive TIMEOUT is the only real guarantee - a refresh, a killed tab or a crashed browser never sends a stop. The page checks in every 15s and listeners.js reaps at 60s; the pagehide stop is best-effort on top. A poll that exits on its own clears the entry and a keepalive NEVER re-arms, or a missing lavish-axi (poll exits instantly) becomes a respawn loop for as long as the review is on screen.  _(2026-07-30T05:43:30Z)_
