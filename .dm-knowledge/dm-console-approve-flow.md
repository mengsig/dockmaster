- **[invariant]** The console's review index route (/review/<id>/) now serves a SERVER-AUTHORED wrapper (ui/server.js reviewShellHtml/reviewShellCsp), not the raw artifact: the artifact still renders unchanged (same reviewCsp, connect-src 'none', still reached at /review/<id>/<basename> via the pre-existing asset branch of serveReview), embedded in the wrapper via an iframe carrying `sandbox="allow-scripts"` (no allow-same-origin). Only the wrapper - never influenced by artifact content - gets script-src/connect-src 'self', which is what lets its notes box (ui/public/review.mjs) enqueue Approve/Request-changes messages through the same /api/chat queue as the composer (ui/public/api.mjs now holds the one shared postMessage(), imported by both console.mjs and review.mjs). The isolation guarantee is the sandbox's opaque origin, not the CSPs: a same-origin iframe with no sandbox would hand the artifact a SAME-ORIGIN window.parent, letting its inline scripts call parent.fetch under the wrapper's connect-src 'self' or simply click the wrapper's own Approve/Send buttons - a CSP is per-document and cannot stop that. State the guarantee precisely: an opaque-origin frame DOES still get a window.parent (a cross-origin WindowProxy, so parent.postMessage is reachable and the shell simply listens for nothing). What it does not get is anything that crosses the origin - no DOM access (parent.document throws, contentDocument reads back null), no parent.fetch, no same-origin reach. Do not relax reviewCsp() itself to add artifact interactivity - that would let the artifact's own (crewmate-authored, less trusted) markup reach the API; add capability to the wrapper document instead. Never add allow-same-origin to the iframe's sandbox attribute - that reinstates the hole.  _(2026-07-27T13:16:59Z)_
- **[convention]** Needs-you's aggregated review row (live.js toNeedsYou, kind 'review') now carries an items[] array ({title, repo, review_href} per waiting task, no id) alongside the aggregate count/repos/href - views.mjs berth()/reviewRow() renders one Approve/Request-changes pair per task from it, via the same approveControl()/changesControl() voyage() uses. dom.mjs's askControl() grew an optional notes mode (spec.notes:true + spec.buildRequest(notes)) for a request that needs free text first (Request changes) - existing canned-string callers (trash, cleanup) are untouched.  _(2026-07-27T13:17:07Z)_
- **[invariant]** The review shell's iframe carries sandbox="allow-scripts" with no allow-same-origin, so the artifact renders into an OPAQUE origin: localStorage/sessionStorage/document.cookie all throw SecurityError there, not just window.parent access. An artifact template that reaches for browser storage breaks itself in this sandbox, not just in the isolation guarantee - crewmate-authored artifact templates must not touch storage.  _(2026-07-27T18:19:55Z)_
- **[invariant]** The console-served review shell (/review/<id>/) frames the artifact with sandbox="allow-scripts", i.e. an OPAQUE origin, so EVERY subresource the artifact requests (screenshots, its own stylesheet, fonts) arrives at the server with sec-fetch-site: cross-site - the browser has no choice about that. So a cross-site refusal under /review/ must be scoped to DOCUMENT loads (ui/server.js isDocumentLoad(): sec-fetch-dest document/iframe/frame/embed/object, missing dest counted as a document): applied to the whole prefix it 403s every screenshot in every review page. Refusing document loads alone still refuses a hostile framing of the shell AND of the artifact, and gives up nothing (the artifact document has no controls and connect-src 'none'). Any test for this must SEND the fetch-metadata headers - tests/check-console-http.js sent none, which is how the break stayed green.  _(2026-07-27T19:26:12Z)_
- **[invariant]** Approve/Request-changes may be offered ONLY while the work is awaiting review, and that now holds on BOTH documents: the fleet page's cards (ui/public/views.mjs approveControl/changesControl gate on item.state === 'ready_for_review' plus review_href) and the console-served review shell (ui/live.js taskInfo() now also runs dm-task.sh state <id> and maps it through STATE_WORDS; ui/server.js reviewShellHtml crosses it as the single boolean `awaiting`; ui/public/review.mjs draws the controls only when it is true, otherwise a worded 'no longer waiting on your review' line). The shell needs the gate because a rendered artifact OUTLIVES the work - the Reviews panel keeps landed/dropped reviews and its rows reach the shell through /api/review-open?...&redirect=1, whose fallback is /review/<id>/ - so 'there is an artifact' is never evidence a decision is still open. Read the RECONCILED state (dm-task.sh state), not a meta field: that is what notices an out-of-band merge.  _(2026-07-27T19:26:22Z)_
