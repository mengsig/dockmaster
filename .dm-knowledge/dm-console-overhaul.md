- **[pitfall]** ui/poll.js must never process.exit() straight after process.stdout.write: stdout is ASYNC when it is a pipe on macOS, so exit() discards the unflushed tail - and the claim rename has already happened, so the message is gone. Acknowledge inside the write callback and let the loop drain instead.  _(2026-07-26T19:56:28Z)_
- **[invariant]** The console chat queue is three atomic renames: state/ui/inbox -> claiming/<pid>-<name> (the claim) -> claimed/<name> (acknowledged, only after the text is out of stdout). A claim whose pid is dead, or older than chat.STALE_CLAIM_MS, is renamed back to inbox. Loss is impossible; one duplicate delivery is possible if a poller dies between printing and acknowledging - that is the direction to err in.  _(2026-07-26T19:56:28Z)_
- **[pitfall]** Chrome keeps ui/public/console.css in its MEMORY cache across navigations despite cache-control: no-store, so a CSS edit does not show up on reload. When iterating on the console's design, bust it: set the stylesheet link's href to /console.css?v=<now>. Screenshots taken without that show the previous stylesheet.  _(2026-07-26T19:56:29Z)_
- **[convention]** The console document carries NO task id on purpose (live.js toWork omits it, reviews refuse to fall back to one), so page controls that name work - the trash request - identify it by title + repo. tests/check-console.js pins that no work or needs-you row has an id.  _(2026-07-26T19:56:29Z)_
