- **[invariant]** git -C <path> WALKS UP past a missing/broken .git and silently answers from the enclosing repo (state/worktrees/ sits inside DM_HOME's own tree, so it lands on the distro). Every read of a task worktree in dm-trash.sh/dm-worktree.sh (landed(), preserve_discarded_head(), dm-trash's snapshot+dirty-summary) must call dm_worktree_contained/dm_assert_worktree_contained (dm-lib.sh) FIRST and treat a failure as undetermined/refused, never fall through to git's answer. Root cause of #210/#181/#209.  _(2026-07-30T05:19:39Z)_
- **[pitfall]** Reproducing the walk-up bug in tests needs the worktree's ENCLOSING directory tree to be a real git repo (git init on $DM_HOME) — a throwaway smoke-suite DM_HOME is not one by default, so rm -f "$wt/.git" alone (as the pre-existing tr-admin fixture does) only exercises the safe 'git errored, fall back to admin record' path, never the actual defect.  _(2026-07-30T05:19:39Z)_
