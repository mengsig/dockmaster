- **[pitfall]** ui/public: the console rebuilds the whole panel (main.textContent='' then re-render) every 30s refresh AND every 60s clock tick, so ANY css entrance animation on a child element re-fires on a timer - which trains the operator to ignore movement and costs the one channel that should mean 'look here'. Motion must be gated on real change: a node declares data-key (stable identity) and data-sig (state), and markChanges() in dom.mjs diffs against the previous render of that panel. Neither attribute may contain a relative age or the 60s re-render marks everything changed. data-key is NOT unique - two open tasks in one repo can share a title - so markChanges suffixes repeats by render order; without that the duplicates collapse onto one map key and re-animate forever on unchanged data.  _(2026-07-26T17:22:44Z)_
- **[convention]** Designing ui/public against the demo fleet: state.js re-reads ui/fixtures/console.json on EVERY collect, so editing that file and hitting Refresh in the page exercises any state - degraded panels, a PR flipping green - with no live fleet and no server restart. Revert it with git checkout before committing.  _(2026-07-26T17:22:44Z)_
- **[convention]** Serving a private console instance beside the operator's live one: `DM_HOME=<scratch> DM_UI_PORT=<49xx> bin/dm-ui.sh start --source fixture` is safe. dm-ui.sh resolves code from the script but its RUN_DIR is `$DM_STATE/ui`, which follows DM_HOME - so the scratch instance writes its own server.pid and never touches the live 4877 one. Stop it by the pid in `<scratch>/state/ui/server.pid`, never by process name. (Supersedes an earlier note claiming dm-ui.sh owns a single shared pid file and must be bypassed with `node ui/server.js`.)  _(2026-07-30T00:00:00Z)_
- **[convention]** ui/public/console.css: the blanket `@media (prefers-reduced-motion: reduce)` rule kills `animation` and `transition` on `*`, so a new keyframe animation inherits reduced-motion compliance for free - but anything whose only visible resting state came FROM the animation needs a static fallback inside that block (see `body.is-refreshing .topbar::after`).  _(2026-07-30T00:00:00Z)_
- **[pitfall]** tests/check-console.js walks the real rendered tree through a hand-rolled fake `document` (withCapturingDocument / withStubDocument). It implements only what views.mjs and dom.mjs actually call, so adding a new DOM API to the page - `dataset`, for one - fails every panel test until the stub grows it too.  _(2026-07-30T00:00:00Z)_
