- **[invariant]** DM_HOME is canonicalized (`pwd -P`) at assignment in dm-lib.sh and must stay that way: git records worktree paths PHYSICALLY, so a verbatim DM_HOME (macOS TMPDIR /var -> /private/var, any symlinked root) makes every stored-path-vs-git comparison miss by string. The failure mode is SILENT and destructive - clear_missing_worktree's admin-record lookup matched nothing, wrote "no git record held a head", and pruned a real commit into unreachability while reporting it as nothing at risk. Do not move this canonicalization to a caller and do not drop it as redundant.  _(2026-07-22T06:12:00Z)_
- **[pitfall]** That canonicalization is CONDITIONAL on DM_HOME already existing, and must stay conditional: tests and first runs point DM_HOME at a directory dm_ensure_dirs has not created yet, so an unconditional `cd` there breaks every fresh run. It is still sound because the only writer of an absolute DM_HOME-derived path (dm-worktree.sh create, the `worktree` meta field) requires an already-registered repo - by the time any path is RECORDED the directory exists, so the recorded value is canonical. The registry is immune by construction: it stores `repos/<name>` relative and joins at read time.  _(2026-07-22T06:12:00Z)_
- **[convention]** tests/scout-cleanup.sh runs the WHOLE matrix through a symlinked root on purpose (mkdir real; ln -s real link; DM_HOME=$TMP/link/home). Under a canonical root the three commit-destruction checks are INVISIBLE - the suite passed 88/88 with the bug wide open. Any new case comparing a recorded path against git's own record must hold under both roots: plant a non-canonical path via the suite's own symlink alias, never via DM_HOME, or the precondition silently holds only under the symlinked root and the case tests nothing. `set -euo pipefail` means a failed `ln -s` aborts rather than degrading to canonical.  _(2026-07-22T06:12:00Z)_
- **[convention]** clear_missing_worktree looks the admin record up by $DM_WT/<id> FIRST and falls back to the stored path. The derived key is canonical by construction; a record written BEFORE the DM_HOME fix holds a symlinked path, and its directory is already gone by then so it cannot be canonicalized at read time. Keep that order - the fallback alone reopens silent destruction for pre-upgrade records.  _(2026-07-22T06:12:00Z)_
- **[invariant]** Every --force path parks the head BEFORE destroying anything: refs/dm-discarded/<sanitized-id>/<sha> in the CLONE (shared object store). Keyed by sha because ids are reusable and refs/dm-discarded/* gets NO reflog (git auto-logs only heads/remotes/notes/HEAD), so a per-id ref clobbers an earlier discard into unreachability. All-zeros oids are refused before parking - update-ref reads zeros as DELETE and exits 0, creating nothing while reporting success. A vanished DIRECTORY does not take the commit with it: git's admin record is its last reference, and `worktree prune` is what drops it.  _(2026-07-22T06:12:00Z)_
