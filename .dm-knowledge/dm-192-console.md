- **[invariant]** The console (bin/dm-ui.sh + ui/) reads fleet state ONLY through the dm-* scripts. ui/state.js is the single seam: it defines the one document the page renders and picks the source (fixture or live). Nothing under ui/ opens state/repos.json, state/tasks/*.meta or state/backlog.json - if a panel needs a field, add a --json output to the owning script.  _(2026-07-25T19:59:24Z)_
- **[convention]** The console serves on 127.0.0.1:4877 (DM_UI_PORT overrides), chosen clear of lavish 4387, chrome-devtools 9224, and the verify gate's app 8600-8999 / bridge 9300-9499 / CDP 9600-9799 ranges.  _(2026-07-25T19:59:24Z)_
- **[pitfall]** Any DM_* environment override a bin/ script READS also needs a row in the README override table - tests/smoke.sh derives the list from bin/ and fails if the README is missing one (#113). Caught DM_UI_PORT.  _(2026-07-25T19:59:24Z)_
- **[pitfall]** An author CSS `display` rule beats the UA stylesheet's `[hidden]{display:none}` outright (author beats UA regardless of specificity), so any element toggled via the hidden ATTRIBUTE that also carries a display in console.css must opt back out with an explicit `.x[hidden]{display:none}`. Cost: the chat pending row rendered permanently with an empty label and a pulsing lamp.  _(2026-07-25T21:18:31Z)_
- **[pitfall]** dm-task.sh's reconcile detail is written FOR the dockmaster - 'reported ready but not yet landed', 'lavish artifact ready for the operator', local-copy paths. It must never be rendered as prose in the console; ui/live.js turns it into a token (unlanded|not_started|undeterminable) and only a blocked/failed task's own named blocker crosses as free text. Same for dm-memory.sh recall, whose output carries section banners, git-excluded file paths and HTML comments around the notes.  _(2026-07-25T21:18:31Z)_
- **[invariant]** The console's progress track never claims more than was recorded: a stage with no evidence is 'ahead' (not reached), a stage on a task whose state could not be reconciled is 'unknown', and a declared gate that records nothing is 'unrecorded'. Only tests/verify leave a per-gate record, so the review and fix gates are shown as a plan (what must still be cleared), never as passed. tests/check-console.js pins all three distinctions.  _(2026-07-25T21:18:31Z)_
- **[convention]** ui/ code resolves from the SCRIPT's location (BASH_SOURCE/../ui), never from DM_HOME - that is what lets a worktree's console serve the real home's fleet during development (DM_HOME=<real> bin/dm-ui.sh start). Only runtime files (state/ui/) follow DM_HOME. DM_BIN is passed to the server explicitly so it knows which toolbelt to shell out to.  _(2026-07-25T21:18:31Z)_
- **[pitfall]** ui/live.js must use async execFile, never execFileSync: reconciling 90 tasks takes ~8s and the PR sweep is a network walk, and a synchronous child freezes the chat long-poll and every other request for exactly that long. Independent sources also run under Promise.all - the task reconcile alone is most of the wall time.  _(2026-07-25T21:18:31Z)_
- **[convention]** Adding a skill requires re-baselining config/runtime-performance-baseline.json: claude_skill_bytes and claude_skill_description_bytes are EXACT equality checks and claude_files_sha256 is an exact inventory. Description bytes are counted as '<dir>: <description>\n' per skill, not the raw field. Do NOT bump shared_agents_bytes for an AGENTS.md addition that fits inside the +2048 allowance - that floor is a ratchet, and base_commit names the change that moved it.  _(2026-07-25T21:18:31Z)_
- **[pitfall]** GNU 'stat -f' rejects a BSD format string but still PRINTS filesystem info for the file operand, so the common 'stat -f %z f 2>/dev/null || stat -c %s f' portability idiom CONCATENATES garbage with the real value on Linux. Capture the BSD attempt into a variable, discard it unless it exited 0, and validate the result is all-digits. tests/smoke.sh's file_mode helper has the same latent shape.  _(2026-07-25T21:18:31Z)_
- **[invariant]** Binding 127.0.0.1 and checking the Host header is NOT CSRF protection. Host defeats DNS rebinding (a cross-origin READ); it does nothing against a cross-site WRITE aimed at the literal loopback URL, because the browser sets Host itself. A text/plain POST is a CORS-SIMPLE request: no preflight, sent cross-origin, 201. Proven against dockmaster's console in a real browser. Any loopback endpoint that mutates needs: Sec-Fetch-Site in {same-origin,none} (a forbidden header name, unforgeable by page script), an Origin allowlist, AND a non-simple content-type (application/json) so a scripted cross-origin attempt must preflight and the preflight is refused. Note a page on another PORT of 127.0.0.1 sends sec-fetch-site: same-site, not cross-site - allowing 'same-site' would reopen it.  _(2026-07-26T09:21:57Z)_
- **[pitfall]** A state-changing GET is a CSRF vector too. dockmaster's console guarded POST /api/chat but left GET /api/state?refresh=1, which drives a fleet-wide dm-pr.sh sweep that WRITES pr_state/checks and takes the task lock - reachable from a hostile page with a bare <img src>. Audit every GET that has a side effect, not just the write verbs.  _(2026-07-26T09:21:57Z)_
- **[pitfall]** A callback registered OUTSIDE a request handler (fs.watchFile, setTimeout) has no catch above it: a throw there is an uncaught exception and exits the node process. dockmaster's console died on one malformed line in state/ui/chat.jsonl because the long-poll wake path called a strict JSON reader from a watchFile callback. Two rules: contain every such callback in its own try/catch, and for a file TWO processes append to, count unreadable lines and report the count rather than throwing - a strict reader makes one torn byte permanently fatal.  _(2026-07-26T09:21:57Z)_
- **[convention]** A panel whose data source failed must say so IN THAT PANEL, and may never render a reassuring empty state. dockmaster's console collector emits degraded[] as {source, panel, subject} TOKENS - the script name, its argv and its stderr go to the process log only - and each view renders the losses for its own panel, suppresses 'All clear'/'Nothing is waiting to land', and drops the rail count rather than showing a confident 0. Also: a collector that RESOLVES on failure gets its empty result cached for the whole TTL; reject instead, and signal unreadable as an explicit null the document builder asserts on.  _(2026-07-26T09:21:57Z)_
- **[pitfall]** WHATWG URL normalises dot segments BEFORE a server route sees them, including percent-encoded ones: %2e%2e, .%2e and %2e. are all treated as '..'. So new URL(req.url,base).pathname has already collapsed /a/b/../.. - a path-traversal test asserting 400 on those will fail because the route never sees them. Test the property (nothing outside the directory is reachable), not one status code, and keep the per-segment name test as the real defence.  _(2026-07-26T09:21:57Z)_
- **[pitfall]** A READ endpoint that refreshes a cache is state-changing when the collection behind it writes. dockmaster's console guarded GET /api/state?refresh=1 but not the plain GET /api/state - which runs the identical collection whenever its TTL lapses, and that collection records each PR's state via dm_meta_set and takes dm_lock. So <img src='http://127.0.0.1:PORT/api/state'> on a hostile page contended for the task lock every 20s and drove a full GitHub sweep every 150s, with no query string and no legacy browser needed. Guard the ROUTE, not the expensive-looking parameter. Also: a test asserting the unguarded shape 'is still served' pins the hole as intended behaviour - check what the existing tests claim before trusting them.  _(2026-07-26T10:51:40Z)_
- **[pitfall]** When fixing an uncaught-in-callback bug, fix EVERY process that registers the same kind of callback, not the one in front of you. dockmaster's console had the pattern twice: ui/server.js (fs.watchFile -> a strict JSON read) and ui/poll.js (fs.watch + setInterval -> chat.claimOldest -> JSON.parse). Fixing only the server left one malformed inbox file able to exit the poller - which IS the dockmaster's wake mechanism, so the message survived in the queue exactly as documented while nothing was left listening and nothing restarts it. Same family as the enumerate-every-caller rule.  _(2026-07-26T10:51:40Z)_
- **[pitfall]** A script that exits NONZERO to report its answer, printing that answer on STDOUT, breaks any caller that builds its error from stderr. node's execFile rejection carries both err.stdout and err.stderr; a wrapper that keeps only stderr silently discards the answer. Bit dockmaster's console twice: dm-doctor.sh check --json (verdict on stdout, exit 1 when a tool is missing) made the health panel blank in exactly the case it existed to describe, and dm-worktree.sh tangle (exit 1 = tangled) made a tangled clone render as 'On main'. Two fixes: carry err.stdout on the wrapped error, and for a NEW --json flag choose exit 0 whenever the question could be answered, putting the answer in the object.  _(2026-07-26T10:51:40Z)_
- **[invariant]** CSP 'self' is the whole ORIGIN, so it does not isolate one path from another. dockmaster's archived review pages are served same-origin with 'unsafe-inline', and img-src/style-src/font-src 'self' let an inline script in a review page fire GET /api/state?refresh=1 in a loop via <img>, <link rel=stylesheet> or @font-face - a same-origin request no cross-site check can refuse. Scope every fetch directive to the subtree instead (http://127.0.0.1:PORT/review/), which keeps screenshots working and makes the API unreachable from a review page by any means. connect-src 'none' alone is not enough: it only covers fetch/XHR/WebSocket.  _(2026-07-26T10:51:40Z)_
- **[convention]** A same-tab NAVIGATION that fails has no page script to word the error - the browser renders whatever the server returned as the body. So a route reached by navigation (dockmaster's /review/<id>/) must return an already-worded HTML page, never the JSON error shape an XHR route uses; otherwise script names, argv, errno and absolute paths become the page. Word every arm, including the catch-all fallback: dockmaster's ENOENT arm was worded and its  fallback leaked ENOTDIR plus the archive's absolute path, reachable by appending a segment to any real file.  _(2026-07-26T10:51:40Z)_
