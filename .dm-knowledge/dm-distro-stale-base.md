- **[pitfall]** dm-worktree.sh create special-cases repo==dockmaster: dm-sync.sh deliberately refuses the distro's own clone, so its local default branch only advances when the operator pulls by hand and the FF-sync freshness guard is a permanent no-op there. create fetches origin's default branch into a PRIVATE refs/dm/base/<def> and cuts the worktree off the commit that fetch wrote. Not local main (stale), not the remote-tracking ref (a hand-narrowed remote.origin.fetch leaves origin/<def> stale even after a successful explicit fetch), and NOT FETCH_HEAD -- that is one scratch file per clone which ANY fetch rewrites, so a concurrent fetch of another ref against the same clone yields an unrelated branch tip printed as 'base: origin/main @<sha>'. Fetching into an explicit named destination is what removes the window.  _(2026-07-29T13:50:37Z)_
- **[invariant]** In dm-worktree.sh's distro arm every wrong-base condition is dm_die, never dm_warn: failed fetch, refs/dm/base/<def> not resolving, and a local default branch carrying commits origin lacks (which would be silently dropped). Local branches are addressed as refs/heads/<def>, never bare <def>, because git rev-parse searches refs/tags/ BEFORE refs/heads/ -- a tag named after the default branch otherwise answers the divergence question and lets unpushed commits through. Same convention as dm-sync.sh's show-ref --verify.  _(2026-07-29T13:50:37Z)_
- **[pitfall]** The distro freshness guarantee covers DEFAULT-BRANCH worktrees only. dm-worktree.sh create --base (stacked sub-PR children) is evaluated BEFORE the distro arm, so it still dm_warns on a failed fetch and still resolves origin/<base_ref>, which a narrowed refspec can leave stale. dm-worktree.sh landed best-effort-fetches then reads origin/<def> the same way; a stale read there reports landed work as unlanded and blocks cleanup. Both are pre-existing and unfixed.  _(2026-07-29T13:50:37Z)_
- **[convention]** dm-worktree.sh create prints TWO stdout lines: 'base: <ref> @<sha>' then the worktree path. The header at bin/dm-worktree.sh:22-25 is the single owner of that contract ('consume only the LAST line'); the skills that show the command do not restate it, deliberately -- a second copy is a second thing to go stale.  _(2026-07-29T13:51:00Z)_
